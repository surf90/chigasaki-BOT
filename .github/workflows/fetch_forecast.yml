name: Fetch JMA Forecast

on:
  schedule:
    # JST 4:00, 10:00, 16:00, 22:00 -> UTC 19:00, 1:00, 7:00, 13:00
    - cron: '0 19,1,7,13 * * *'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  update-forecast:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Run fetch script
        run: python scripts/fetch_forecast.py
      - name: Commit and push if changed
        run: |
          git config --global user.name 'github-actions[bot]'
          git config --global user.email 'github-actions[bot]@users.noreply.github.com'
          git add data/forecast_data.json
          git diff --quiet && git diff --staged --quiet || (git commit -m "Update forecast data" && git push)
